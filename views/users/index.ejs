<!DOCTYPE html>
<html>
  <head>
    <title>Users</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Users (<%= users.length %>) </h1>
    <ul>
      <% users.forEach(user => { %>
        <li><%= user.name %></li>
       <% }) %>
    </ul>
  </body>
</html>
